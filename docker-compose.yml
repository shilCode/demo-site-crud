services:
  app:
    build: .
    ports:
      - "4001:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
    volumes:
      - db-data:/app/server/prisma
    restart: unless-stopped

volumes:
  db-data:
